{
  "name": "faceprep-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd faceprep && npm install && npm run build && cd ../faceprep-backend && npm install",
    "start": "node faceprep-backend/src/server.js"
  },
  "engines": {
    "node": "20.x"
  }
}